version: '3.7'
services:
  web:
    links:
    - 'mock'
    volumes:
      - type: bind
        source: './php_utils'
        target: /var/www/html/magento/shell/drip
  db:
    tmpfs: /var/lib/mysql
  mock:
    image: jamesdbloom/mockserver
    ports:
    - 1080:1080
